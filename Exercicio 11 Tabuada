#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <string.h>

//Exercicio 11

main(){
	
	int fator1  = 0,
	    fator2  = 0,
	    produto = 0;
	    
	    
	    for(fator1 = 1; fator1 <= 10; fator1++)
	    {
	    	printf("Tabuada do %d", fator1);
	    
	        for(fator2 = 1; fator2 <= 10; fator2++)
		{
	       
	        	produto = fator1 * fator2;
	        
	        	printf("%d * %d = %d", fator1, fator2, produto);
	    	
	  	}
	    return 0;
}
